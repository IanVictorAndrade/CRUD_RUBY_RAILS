
$(".product-<%=params['cart_id'] %>").remove();
var count = $('.cart-table tr').length;
$('.cart_count').text(count);
if (window.toastr) {
    toastr.success('Product removed from cart successfully')
}
window.updateTotalPrice();
